import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  participant: {
    marginTop: 20,
    flexDirection: 'row',
    justifyContent: 'space-between',
  },

  bottomSpace: {
    paddingBottom: 20,
  },

  progress: {

  }
})
